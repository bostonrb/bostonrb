#new
  .ribbon
    .row
      %h2 New project
      = error_messages_for :project

      - form_for(@project) do |form|
        .fieldset
          = partial 'projects/form', :form => form
        .buttons
          = form.submit 'Create', :disable_with => 'Please wait...'
