%h2 Edit job
= error_messages_for :job

- form_for(@job) do |form|
  = partial 'jobs/form', :form => form
  %p= "#{form.submit 'Update', :disable_with => 'Please wait...'}"
  %p= link_to 'Delete', job_path(@job), :method => :delete, :confirm => 'Are you sure?'