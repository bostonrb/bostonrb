.row
  %h2 Jobs
  %p This is a free service for Boston Rubyists & those seeking Boston Rubyists.
  %p= link_to 'Add a job', new_job_path

%h3 Job Postings
- unless @jobs.empty?
  - @jobs.each do |job|
    = partial 'jobs/list_item', :job => job
- else
  %p None at this time.
= link_to "#{@recent_job_count} recent job posts", jobs_path if @recent_job_count
