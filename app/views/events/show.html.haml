- content_for :google_maps_js do
  = event_map @event
  
= partial 'layouts/full' do
  %h2= @event.title

  = partial 'events/show_in_feed', :event => @event
  
  %p= link_to 'Edit', edit_event_path(@event)
  
  #map
