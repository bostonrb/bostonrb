%h2 Edit event
= error_messages_for :event

- form_for(@event) do |form|
  = partial '/events/form', :form => form
  %p= "#{form.submit 'Update', :disable_with => 'Please wait...'}"
  %p= link_to 'Delete', event_path(@event), :method => :delete, :confirm => 'Are you sure?'